import React from 'react';
import './App.css'; // This line imports your application's CSS file
import Navbar from './components/navbar/navbar'; // Imports the about component
import About from './components/about/about'; // Imports the contact component
import Projects from './components/projects/projects'; // Imports the footer component
import Home from './components/home/home'; // Imports the header component
import Contact from './components/contact/contact'; // Imports the projects component
import { Nav } from 'reactstrap';

function App() {
  return (
    <div className="App">
      

      
    </div>
  );
}

export default App;
