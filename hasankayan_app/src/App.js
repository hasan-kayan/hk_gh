import React from 'react';
import './App.css'; // This line imports your application's CSS file
import About from './components/navbar/navbar'; // Imports the about component
import Contact from './components/about/about'; // Imports the contact component
import Footer from './components/projects/projects'; // Imports the footer component
import Header from './components/home/home'; // Imports the header component
import Projects from './components/contact/contact'; // Imports the projects component

function App() {
  return (
    <div className="App">
      
      
    </div>
  );
}

export default App;
